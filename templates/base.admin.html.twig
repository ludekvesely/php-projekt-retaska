{% extends 'base.html.twig' %}

{% block nav %}
    <nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
        <a class="navbar-brand" href="{{ path('admin_index') }}">Re-taška: Administrace</a>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('product_index') }}">Produkty</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('category_index') }}">Kategorie</a>
                </li>
            </ul>
            <ul class="navbar-nav my-2 my-sm-0">
                <li class="nav-item nav-dark">
                    <span class="nav-link">Přihlášený uživatel: {{ app.user.username }}</span>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Odhlásit</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('product_list_index') }}">Zobrazit frontend</a>
                </li>
            </ul>
        </div>
    </nav>
{% endblock %}
